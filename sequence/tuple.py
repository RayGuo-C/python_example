# tuple is one of the sequences and each sequence has values and relevant index
# the major difference between tuple and list is that tuple cannot be changed
# Create the tuple is to input the comma-seperated data between the parentheses
# It is safe to save the data due to its invariable.

num = (3, 12, 4)
num_slice = num[1:] # after slice, it is also a tuple
# num[1] = 4
print(num[1])
print(num_slice)
